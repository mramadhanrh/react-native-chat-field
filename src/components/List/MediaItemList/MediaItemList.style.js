import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  listRow: {
    flexDirection: 'row',
    justifyContent: 'space-between'
  },

  icon: {
    color: '#ffffff',
    fontSize: 24
  }
});
